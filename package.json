{
  "name": "uv-template",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "lint-staged": "lint-staged --concurrent false --no-stash --no-hide-partially-staged",
    "prepare": "test \"$CI\" = 'true' || husky",
    "pre-commit": "just pre-commit"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.3.0",
    "prettier": "^3.9.6"
  }
}
